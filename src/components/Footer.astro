<footer class="site-footer">
  <div class="container py-12">
    <div class="footer-grid">
      <div class="footer-brand">
         <a href="/" class="brand">MH</a>
         <p class="text-muted mt-4 max-w-xs">Building premium digital experiences for forward-thinking businesses.</p>
      </div>
      
      <div class="footer-links">
         <div class="link-group">
            <h4>Work</h4>
            <a href="/projects">Projects</a>
            <a href="/services">Services</a>
         </div>
         <div class="link-group">
            <h4>Social</h4>
            <a href="#">X / Twitter</a>
            <a href="#">LinkedIn</a>
            <a href="#">GitHub</a>
         </div>
      </div>
    </div>
    
    <div class="footer-bottom mt-12 pt-8 border-t border-color-border">
      <p class="text-muted text-xs">© {new Date().getFullYear()} MH Portfolio. Built for performance.</p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--color-border);
    margin-top: var(--space-12);
    background: var(--color-bg);
  }

  .footer-grid {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: var(--space-10);
  }

  .brand {
    font-size: 1.25rem;
    font-weight: 700;
    letter-spacing: -0.04em;
    color: var(--color-text-primary);
    text-decoration: none;
  }

  .footer-links {
    display: flex;
    gap: var(--space-12);
  }

  .link-group h4 {
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin-bottom: var(--space-4);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .link-group a {
    display: block;
    font-size: 0.9rem;
    color: var(--color-text-secondary);
    text-decoration: none;
    margin-bottom: var(--space-2);
    transition: color 0.2s ease;
  }

  .link-group a:hover {
    color: var(--color-text-primary);
  }

  .text-muted { color: var(--color-text-muted); }
  .text-xs { font-size: 0.75rem; }
  .mt-4 { margin-top: 1rem; }
  .mt-12 { margin-top: 3rem; }
  .pt-8 { padding-top: 2rem; }
  .max-w-xs { max-width: 20rem; }
  .border-t { border-top: 1px solid var(--color-border); }

  @media (max-width: 640px) {
    .footer-grid { flex-direction: column; }
    .footer-links { gap: var(--space-8); }
  }
</style>